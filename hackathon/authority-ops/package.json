{
  "name": "authority-ops",
  "version": "0.1.0",
  "private": false,
  "description": "A professional Strands agent that performs invoice operations inside explicit delegated authority.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "demo": "tsc -p tsconfig.demo.json && node dist-demo/demo.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json",
    "live": "node --enable-source-maps dist/live-agent.js",
    "test": "tsc -p tsconfig.demo.json && node dist-demo/test.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@strands-agents/sdk": "^1.0.0",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.8.3"
  }
}
